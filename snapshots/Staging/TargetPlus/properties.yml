version: 2
models:

  - name: TargetPlus_Orders
    config:
      materialized : incremental
      incremental_strategy: merge
      unique_key : ['Date_Placed' , 'TCIN']
      partition_by : {'field': 'Date_Placed', 'data_type': 'timestamp'}
      cluster_by : ['Date_Placed' , 'TCIN']
